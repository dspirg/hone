---
phase: 12-app-store-submission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WorkoutApp/PrivacyInfo.xcprivacy
  - WorkoutApp/Assets.xcassets/AppIcon.appiconset/Contents.json
  - Config/Prod.xcconfig
  - docs/privacy-policy.html
autonomous: true
requirements: [SHIP-01, SHIP-02, SHIP-04, SHIP-06]

must_haves:
  truths:
    - "PrivacyInfo.xcprivacy exists and declares UserDefaults access with reason CA92.1"
    - "PrivacyInfo.xcprivacy declares collected data types for purchase history and email"
    - "AppIcon Contents.json references AppIcon-1024.png filename"
    - "Prod.xcconfig has real Supabase URL and anon key (same as Dev since single project)"
    - "Privacy policy HTML page exists with all required disclosures"
  artifacts:
    - path: "WorkoutApp/PrivacyInfo.xcprivacy"
      provides: "Apple privacy manifest for App Store compliance"
      contains: "NSPrivacyAccessedAPICategoryUserDefaults"
    - path: "WorkoutApp/Assets.xcassets/AppIcon.appiconset/Contents.json"
      provides: "App icon catalog entry with filename reference"
      contains: "AppIcon-1024.png"
    - path: "Config/Prod.xcconfig"
      provides: "Production environment configuration with real Supabase keys"
      contains: "seuzjlqfetbefzdplulz.supabase.co"
    - path: "docs/privacy-policy.html"
      provides: "Publicly hostable privacy policy page"
      contains: "OpenAI"
  key_links:
    - from: "Config/Prod.xcconfig"
      to: "Xcode Release build configuration"
      via: "baseConfigurationReference in project.pbxproj"
      pattern: "SUPABASE_URL.*seuzjlqfetbefzdplulz"
    - from: "WorkoutApp/PrivacyInfo.xcprivacy"
      to: "App bundle"
      via: "Copy Bundle Resources build phase"
      pattern: "NSPrivacyTracking"
---

<objective>
Create all automatable files for App Store submission: privacy manifest, production config, app icon catalog entry, and privacy policy page.

Purpose: These files must exist before the archive build (Plan 02) and App Store listing (Plan 03) can proceed. They represent the code/config foundation that Claude can create without external service access.

Output: Four files ready for integration -- PrivacyInfo.xcprivacy, updated Contents.json, filled Prod.xcconfig, and privacy-policy.html.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-app-store-submission/12-CONTEXT.md
@.planning/phases/12-app-store-submission/12-RESEARCH.md
@.planning/phases/12-app-store-submission/12-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PrivacyInfo.xcprivacy and update Prod.xcconfig</name>
  <files>WorkoutApp/PrivacyInfo.xcprivacy, Config/Prod.xcconfig</files>
  <read_first>
    - Config/Dev.xcconfig (source of truth for xcconfig key format and Supabase values)
    - Config/Prod.xcconfig (current placeholder state)
    - WorkoutApp/WorkoutApp.entitlements (analog for XML plist format in this project)
    - .planning/phases/12-app-store-submission/12-PATTERNS.md (exact file contents for both files)
  </read_first>
  <action>
**PrivacyInfo.xcprivacy** -- Create `WorkoutApp/PrivacyInfo.xcprivacy` with this exact XML content (per D-11, D-12, D-13):

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>NSPrivacyTracking</key>
    <false/>
    <key>NSPrivacyTrackingDomains</key>
    <array/>
    <key>NSPrivacyCollectedDataTypes</key>
    <array>
        <dict>
            <key>NSPrivacyCollectedDataType</key>
            <string>NSPrivacyCollectedDataTypePurchaseHistory</string>
            <key>NSPrivacyCollectedDataTypeLinked</key>
            <true/>
            <key>NSPrivacyCollectedDataTypeTracking</key>
            <false/>
            <key>NSPrivacyCollectedDataTypePurposes</key>
            <array>
                <string>NSPrivacyCollectedDataTypePurposeAppFunctionality</string>
            </array>
        </dict>
        <dict>
            <key>NSPrivacyCollectedDataType</key>
            <string>NSPrivacyCollectedDataTypeEmailAddress</string>
            <key>NSPrivacyCollectedDataTypeLinked</key>
            <true/>
            <key>NSPrivacyCollectedDataTypeTracking</key>
            <false/>
            <key>NSPrivacyCollectedDataTypePurposes</key>
            <array>
                <string>NSPrivacyCollectedDataTypePurposeAppFunctionality</string>
            </array>
        </dict>
    </array>
    <key>NSPrivacyAccessedAPITypes</key>
    <array>
        <dict>
            <key>NSPrivacyAccessedAPIType</key>
            <string>NSPrivacyAccessedAPICategoryUserDefaults</string>
            <key>NSPrivacyAccessedAPITypeReasons</key>
            <array>
                <string>CA92.1</string>
            </array>
        </dict>
    </array>
</dict>
</plist>
```

Key declarations:
- `NSPrivacyTracking = false` (per D-12, no cross-app tracking)
- `NSPrivacyTrackingDomains = []` (per D-13, no tracking domains)
- Collected data: PurchaseHistory (RevenueCat), EmailAddress (Supabase Auth) -- both linked, not for tracking, purpose AppFunctionality
- Accessed API: UserDefaults with reason CA92.1 (per D-11, RevenueCat SDK caching)

**IMPORTANT:** After creating the file, it MUST be added to the Xcode project's WorkoutApp target. Add a PBXFileReference and PBXBuildFile entry in `WorkoutApp.xcodeproj/project.pbxproj` so the file appears in the "Copy Bundle Resources" build phase for the WorkoutApp target. The file reference should use `lastKnownFileType = text.xml` and `sourceTree = "<group>"`.

**Prod.xcconfig** -- Update `Config/Prod.xcconfig` to replace placeholders with real values (per D-15). The Supabase URL and anon key are the same as Dev.xcconfig since there is only one hosted Supabase project. The RevenueCat key remains a placeholder since the production key must come from the RevenueCat dashboard (human action in Plan 03).

New file contents:
```
// Production environment configuration
// RevenueCat key must be replaced with production key from RevenueCat Dashboard before archive

SUPABASE_URL = https:/$()/seuzjlqfetbefzdplulz.supabase.co
SUPABASE_ANON_KEY = eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InNldXpqbHFmZXRiZWZ6ZHBsdWx6Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzY2NTY1NzQsImV4cCI6MjA5MjIzMjU3NH0.DTcyePuQ_JR0YhckdZgo9mGv93OAOc7ZFt20iFpJv7M
REVENUECAT_API_KEY = appl_REPLACE_WITH_PROD_RC_KEY
```

Note: The `https:/$()/` syntax is required xcconfig escaping (prevents `//` from being parsed as a comment). The Supabase anon key is copied from Dev.xcconfig verbatim. The RevenueCat key stays as placeholder -- user fills it from RevenueCat Dashboard in Plan 03.
  </action>
  <verify>
    <automated>grep -q "NSPrivacyAccessedAPICategoryUserDefaults" WorkoutApp/PrivacyInfo.xcprivacy && grep -q "CA92.1" WorkoutApp/PrivacyInfo.xcprivacy && grep -q "seuzjlqfetbefzdplulz" Config/Prod.xcconfig && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - WorkoutApp/PrivacyInfo.xcprivacy contains `NSPrivacyTracking` set to `false`
    - WorkoutApp/PrivacyInfo.xcprivacy contains `NSPrivacyAccessedAPICategoryUserDefaults` with reason `CA92.1`
    - WorkoutApp/PrivacyInfo.xcprivacy contains `NSPrivacyCollectedDataTypePurchaseHistory`
    - WorkoutApp/PrivacyInfo.xcprivacy contains `NSPrivacyCollectedDataTypeEmailAddress`
    - WorkoutApp/PrivacyInfo.xcprivacy contains `NSPrivacyTrackingDomains` with empty array
    - Config/Prod.xcconfig contains `SUPABASE_URL = https:/$()/seuzjlqfetbefzdplulz.supabase.co`
    - Config/Prod.xcconfig contains `SUPABASE_ANON_KEY = eyJhbGciOiJIUzI1NiIs` (starts with this JWT prefix)
    - Config/Prod.xcconfig contains `REVENUECAT_API_KEY = appl_REPLACE_WITH_PROD_RC_KEY` (placeholder retained)
    - PrivacyInfo.xcprivacy is referenced in project.pbxproj Copy Bundle Resources build phase
    - `xcodebuild build -scheme WorkoutApp -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -c "error:"` returns 0
  </acceptance_criteria>
  <done>PrivacyInfo.xcprivacy is created, added to the WorkoutApp target's Copy Bundle Resources, declares UserDefaults CA92.1, purchase history, and email collected data. Prod.xcconfig has real Supabase values and RevenueCat placeholder. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Update AppIcon Contents.json and create privacy policy HTML</name>
  <files>WorkoutApp/Assets.xcassets/AppIcon.appiconset/Contents.json, docs/privacy-policy.html</files>
  <read_first>
    - WorkoutApp/Assets.xcassets/AppIcon.appiconset/Contents.json (current state -- no filename key)
    - WorkoutApp/Core/Theme/Theme.swift (brand colors for privacy policy styling)
    - .planning/phases/12-app-store-submission/12-CONTEXT.md (D-02 icon requirements, D-14 privacy policy requirements)
  </read_first>
  <action>
**Contents.json** -- Update `WorkoutApp/Assets.xcassets/AppIcon.appiconset/Contents.json` to reference the icon PNG filename (per D-02). The PNG file itself will be created by the user in Plan 02 as a design asset. This update prepares the catalog entry so Xcode knows where to look.

New contents (replace entire file):
```json
{
  "images" : [
    {
      "filename" : "AppIcon-1024.png",
      "idiom" : "universal",
      "platform" : "ios",
      "size" : "1024x1024"
    }
  ],
  "info" : {
    "author" : "xcode",
    "version" : 1
  }
}
```

The only change is adding `"filename" : "AppIcon-1024.png"` to the existing images entry.

**Privacy Policy HTML** -- Create `docs/privacy-policy.html` (per D-14). This is a standalone static HTML page suitable for GitHub Pages hosting. It must cover: data collected (email, workout history), AI processing (OpenAI), third-party services (Supabase, RevenueCat, Mux), user rights, and contact information.

Design: Dark background (#0a0a0a), amber (#f59e0b) headings, light text (#e5e5e5), clean typography. Matches Hone brand identity per D-05 warm tone.

App name in policy: "Hone - AI Workout Coach" (per D-03).

Required sections in the HTML:
1. **Information We Collect** -- Email address (account creation), workout history and preferences (session logs, exercise performance, fitness goals, equipment)
2. **How We Use Your Information** -- Personalized workout plans, AI coaching, progress tracking, subscription management
3. **AI Processing** -- OpenAI processes workout plan generation and coaching messages. Workout data is sent to OpenAI's API for plan generation and adaptive coaching. OpenAI does not retain user data for training per their API data usage policy.
4. **Third-Party Services** -- Supabase (database, authentication -- supabase.com), RevenueCat (subscription management -- revenuecat.com), Mux (exercise video delivery -- mux.com), OpenAI (AI plan generation and coaching -- openai.com)
5. **Data Security** -- Encryption in transit (HTTPS), authentication tokens stored in iOS Keychain, row-level security on database
6. **Your Rights** -- Access, correct, or delete your data by contacting us. Account deletion removes all associated data.
7. **Children's Privacy** -- Not directed at children under 13
8. **Changes to This Policy** -- Will update this page with effective date
9. **Contact** -- Email: support@honefitness.com (placeholder -- user can update)
10. **Effective Date** -- April 2026

The HTML should be a complete standalone page with inline CSS (no external dependencies). Include `<meta charset="UTF-8">`, `<meta name="viewport">` for mobile readability, and a `<title>` of "Privacy Policy - Hone".
  </action>
  <verify>
    <automated>grep -q "AppIcon-1024.png" WorkoutApp/Assets.xcassets/AppIcon.appiconset/Contents.json && test -f docs/privacy-policy.html && grep -q "OpenAI" docs/privacy-policy.html && grep -q "RevenueCat" docs/privacy-policy.html && grep -q "Supabase" docs/privacy-policy.html && grep -q "Mux" docs/privacy-policy.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Contents.json contains `"filename" : "AppIcon-1024.png"`
    - Contents.json contains `"idiom" : "universal"` and `"platform" : "ios"` and `"size" : "1024x1024"`
    - docs/privacy-policy.html exists and is valid HTML
    - docs/privacy-policy.html contains "OpenAI" (AI processing disclosure)
    - docs/privacy-policy.html contains "Supabase" (database/auth provider)
    - docs/privacy-policy.html contains "RevenueCat" (subscription management)
    - docs/privacy-policy.html contains "Mux" (video delivery)
    - docs/privacy-policy.html contains "email" (data collection disclosure)
    - docs/privacy-policy.html contains "workout" (data collection disclosure)
    - docs/privacy-policy.html contains "#f59e0b" or "rgb(245, 158, 11)" (brand amber color)
    - docs/privacy-policy.html contains `<meta name="viewport"` (mobile-ready)
  </acceptance_criteria>
  <done>Contents.json references AppIcon-1024.png filename. Privacy policy HTML page exists with all required disclosures covering email, workout data, OpenAI, Supabase, RevenueCat, and Mux. Page is styled with Hone brand colors and ready for GitHub Pages hosting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prod.xcconfig values | Semi-public keys (anon key, public SDK key) embedded in app binary -- acceptable for client-side use |
| Privacy policy page | Public-facing HTML -- no user input, no dynamic content, no trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | Config/Prod.xcconfig | accept | Supabase anon key and RevenueCat public SDK key are designed for client embedding. RLS enforces data isolation server-side. Private repo assumed. |
| T-12-02 | Tampering | docs/privacy-policy.html | accept | Static HTML with no user input; hosted on GitHub Pages with HTTPS. No tampering surface. |
</threat_model>

<verification>
- `xcodebuild build -scheme WorkoutApp -destination 'platform=iOS Simulator,name=iPhone 16'` succeeds with no errors
- `grep "NSPrivacyAccessedAPICategoryUserDefaults" WorkoutApp/PrivacyInfo.xcprivacy` returns a match
- `grep "AppIcon-1024.png" WorkoutApp/Assets.xcassets/AppIcon.appiconset/Contents.json` returns a match
- `grep "seuzjlqfetbefzdplulz" Config/Prod.xcconfig` returns a match
- `docs/privacy-policy.html` exists and contains all four third-party service names
</verification>

<success_criteria>
All four files created/updated. Build succeeds. Privacy manifest declares UserDefaults CA92.1 and collected data types. Prod.xcconfig has real Supabase values. Contents.json is ready for icon PNG. Privacy policy is complete and hostable.
</success_criteria>

<output>
After completion, create `.planning/phases/12-app-store-submission/12-01-SUMMARY.md`
</output>
