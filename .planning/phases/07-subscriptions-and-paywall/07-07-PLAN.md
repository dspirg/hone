---
phase: 07-subscriptions-and-paywall
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - WorkoutApp/Configuration/WorkoutAppProducts.storekit
  - WorkoutApp/Features/Paywall/Retention/DiscountOfferView.swift
autonomous: true
gap_closure: true
requirements:
  - SUBS-01
  - SUBS-04

must_haves:
  truths:
    - "StoreKit config prices match D-01 ($12.99/month), D-02 ($79.99/year), D-12 ($6.49 promo)"
    - "DiscountOfferView body text shows '$6.49/month for 3 months, then $12.99/month' per D-12"
  artifacts:
    - path: "WorkoutApp/Configuration/WorkoutAppProducts.storekit"
      provides: "Correct subscription prices for local StoreKit testing"
      contains: "12.99"
    - path: "WorkoutApp/Features/Paywall/Retention/DiscountOfferView.swift"
      provides: "Specific promotional pricing copy per D-12"
      contains: "$6.49/month for 3 months"
  key_links: []
---

<objective>
Fix pricing configuration and discount offer copy (Gap 3 / D-01, D-02, D-12).

Purpose: StoreKit config has wrong prices ($9.99/$59.99/$4.99 instead of $12.99/$79.99/$6.49) and DiscountOfferView body text uses generic "then your regular price" instead of the specific "$6.49/month for 3 months, then $12.99/month" required by D-12.

Output: Correct prices in StoreKit config and specific pricing copy in DiscountOfferView.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-subscriptions-and-paywall/07-CONTEXT.md
@.planning/phases/07-subscriptions-and-paywall/07-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix StoreKit configuration prices</name>
  <files>WorkoutApp/Configuration/WorkoutAppProducts.storekit</files>
  <read_first>
    - WorkoutApp/Configuration/WorkoutAppProducts.storekit (current wrong prices)
    - .planning/phases/07-subscriptions-and-paywall/07-CONTEXT.md (D-01, D-02, D-12 price decisions)
  </read_first>
  <action>
Edit WorkoutApp/Configuration/WorkoutAppProducts.storekit (JSON file) to update three prices:

1. Monthly subscription `displayPrice`: change `"9.99"` to `"12.99"` (D-01: $12.99/month)
   - This is the `com.workoutapp.pro.monthly` product, currently at line 31

2. Monthly promotional offer `price`: change `"4.99"` to `"6.49"` (D-12: $6.49/month for 3 months)
   - This is the `monthly_50pct_3months` promotional offer, currently at line 55

3. Annual subscription `displayPrice`: change `"59.99"` to `"79.99"` (D-02: $79.99/year)
   - This is the `com.workoutapp.pro.annual` product, currently at line 70

No other fields change. The promotional offer structure (duration P1M, numberOfPeriods 3, offerMode PAY_AS_YOU_GO) stays the same. Trial periods (P14D) stay the same.
  </action>
  <verify>
    <automated>grep '"displayPrice"' WorkoutApp/Configuration/WorkoutAppProducts.storekit && grep '"price"' WorkoutApp/Configuration/WorkoutAppProducts.storekit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "12.99" WorkoutApp/Configuration/WorkoutAppProducts.storekit` matches the monthly displayPrice
    - `grep "79.99" WorkoutApp/Configuration/WorkoutAppProducts.storekit` matches the annual displayPrice
    - `grep "6.49" WorkoutApp/Configuration/WorkoutAppProducts.storekit` matches the promotional offer price
    - No occurrence of "9.99", "59.99", or "4.99" remains in the file
    - JSON is valid (no syntax errors)
  </acceptance_criteria>
  <done>StoreKit configuration prices match D-01 ($12.99/month), D-02 ($79.99/year), D-12 ($6.49 promo)</done>
</task>

<task type="auto">
  <name>Task 2: Fix DiscountOfferView body text with specific pricing</name>
  <files>WorkoutApp/Features/Paywall/Retention/DiscountOfferView.swift</files>
  <read_first>
    - WorkoutApp/Features/Paywall/Retention/DiscountOfferView.swift (current generic text)
  </read_first>
  <action>
Edit WorkoutApp/Features/Paywall/Retention/DiscountOfferView.swift to restore specific pricing copy per D-12.

1. Line 36 -- change the body Text from:
   `Text("Get 50% off for the next 3 months — then your regular price.")`
   to:
   `Text("Get 50% off for the next 3 months — $6.49/month for 3 months, then $12.99/month.")`

2. Line 47 -- change the offer card subtitle from:
   `Text("3 months, then full price")`
   to:
   `Text("$6.49/month for 3 months, then $12.99/month")`

No other changes. The heading "Stay for half price", the "50% off" label, Accept Offer button, and Cancel anyway button all remain unchanged.
  </action>
  <verify>
    <automated>grep "6.49" WorkoutApp/Features/Paywall/Retention/DiscountOfferView.swift && grep "12.99" WorkoutApp/Features/Paywall/Retention/DiscountOfferView.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "6.49" WorkoutApp/Features/Paywall/Retention/DiscountOfferView.swift` returns at least 2 (body text + card subtitle)
    - `grep -c "12.99" WorkoutApp/Features/Paywall/Retention/DiscountOfferView.swift` returns at least 2 (body text + card subtitle)
    - The string "then your regular price" no longer appears in the file
    - The string "then full price" no longer appears in the file
    - Body text contains "$6.49/month for 3 months, then $12.99/month"
  </acceptance_criteria>
  <done>DiscountOfferView displays specific promotional pricing "$6.49/month for 3 months, then $12.99/month" per D-12</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries -- these are configuration and UI copy changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | - | - | - | Configuration and copy changes only; no new attack surface |
</threat_model>

<verification>
1. `grep "12.99" WorkoutApp/Configuration/WorkoutAppProducts.storekit` returns match
2. `grep "79.99" WorkoutApp/Configuration/WorkoutAppProducts.storekit` returns match
3. `grep "6.49" WorkoutApp/Configuration/WorkoutAppProducts.storekit` returns match
4. `grep "6.49/month for 3 months" WorkoutApp/Features/Paywall/Retention/DiscountOfferView.swift` returns match
</verification>

<success_criteria>
All three prices in StoreKit config match CONTEXT.md decisions (D-01, D-02, D-12) and DiscountOfferView shows specific promotional pricing copy.
</success_criteria>

<output>
After completion, create `.planning/phases/07-subscriptions-and-paywall/07-07-SUMMARY.md`
</output>
